version: 2.1
workflows:
  version: 2
  build-deploy:
    jobs:
      - build:
          context: dev
jobs:
  build:
    docker:
      - image: 'circleci/openjdk:8-jdk'
        environment:
          MULE_APP_NAME: demo-sys-salesforce-api
          MULE_ENV: SANDBOX
          MULE_WORKER_TYP: MICRO
          MULE_WORKER: 1
          MULE_REGION: us-east-2
          MULE_OSv2: true
    steps:
      - run: 
          command: |            
             echo "something" \
             echo ${ANYPOINT_USER} \
             echo $ANYPOINT_PASSWORD \
             echo $MULE_APP_NAME \
             echo $MULE_ENV \
             echo $MULE_WORKER_TYP \
             echo $MULE_WORKER \
             echo $MULE_REGION \
             echo $MULE_OSv2 \